{
  "name": "cryptobattleship",
  "version": "1.0.0",
  "description": "CryptoBattleship blockchain game deployment",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:local": "npx hardhat run scripts/deploy.js --network localhost",
    "deploy:sonic": "npx hardhat run scripts/deploy.js --network sonicBlaze",
    "verify:sonic": "npx hardhat run scripts/verify.js --network sonicBlaze",
    "setup:sonic": "npx hardhat run scripts/setup.js --network sonicBlaze"
  },
  "keywords": ["blockchain", "game", "battleship", "nft"],
  "author": "CryptoBattleship Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}